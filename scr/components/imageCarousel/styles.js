import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  root: {
    paddingVertical: 20,
  },
  image: {
    height: 250,
    resizeMode: "contain",
  },
});

export default styles;